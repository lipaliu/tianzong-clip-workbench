# 界面修正第一轮结果（2026-08-13）

## 已经改好的部分

- 主标题“今天要剪哪一场直播？”已回到单行常规大标题字号，不再是两行巨字。
- 顶部步骤条已横向排布，STEP 编号与标题、说明同行，不再逐字竖排。
- 两张上传卡片已并排横向显示，卡片标题为常规 18–22px，说明文字为 13px 正常行宽。
- 字幕路由提示文字已从 10px 提升到 13px，可正常阅读。
- 中缝顶栏英文标语从 9px 提升到 11px。

## 本轮仍然存在的问题（截图实测）

1. **上传阶段内部仍套着一个旧的窄列容器。** 页面中还残留一个左侧竖条区域（内含 STEP 1 / STEP 2 小方块与"选择原片"竖排文字），与新的横向步骤条重复，且把主内容挤到右侧。这是 `.workflow-composer` / `.intake-stage` 内的旧布局仍在生效。
2. **上传卡标题与说明发生重叠。** “上传整场直播，开始找天总切片”被右侧字幕卡覆盖，说明卡片宽度计算仍受旧的绝对定位/负边距影响。
3. **底部三个操作按钮被压成窄竖条**（“选择原片”“选择字幕（可选）”逐字换行），按钮区未使用横向空间。
4. 侧栏“天总专属模型”正文字号偏大，与主区标题层级仍不协调。

下一轮将直接修正 `.workflow-composer`、`.intake-stage`、`.stage-actions` 的布局约束，消除重复的竖列容器与卡片重叠。
